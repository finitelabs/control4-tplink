{
  "private": true,
  "description": "Control4 driver for TP-Link Kasa devices on KLAP firmware (power strips and smart plugs) — npm dependencies only; see Makefile for build targets.",
  "dependencies": {
    "@johnnymorganz/stylua-bin": "^2.3.1",
    "electron-pdf": "^40.1.0",
    "markdown-styles": "^3.2.0",
    "prettier": "^3.8.1"
  }
}
